name: Build Android APK

on:
  workflow_dispatch: # Allows you to run this manually

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          
      # NEW: Force the server to use Java 17 for Android compatibility
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - name: Install Tools
        run: |
          python -m pip install --upgrade pip
          pip install flet
          
      # NEW: Added --verbose so it prints the exact error if it fails
      - name: Build APK via Flet
        run: flet build apk --yes --verbose
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Sales-App-APK
          path: build/apk/**/*.apk
